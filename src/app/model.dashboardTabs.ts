// Layout of each Tab object inside a Dashboard

export class DashboardTab {
    dashboardID: number;                        // FK to Unique DB ID
    widgetTabName: string;                      // Tab Name inside
}