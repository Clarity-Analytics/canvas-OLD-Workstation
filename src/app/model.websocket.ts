// Schema for Websockets messages
// - the Basic is the key, with more detail per type (with different layouts)

// TODO - remove this once model.notification is done and ready
export class SocketMessage {
	message_type: string;
}

